# 📦 发版指南（开发者用）

## 🔄 完整流程一图看懂

```
1. 改代码
2. 改版本号  →  license_mgr.py: CLIENT_VERSION = "v2.7"
3. build_exe.bat 打包
4. 后台 → 🆕 版本发布 → 上传新 exe
5. 用户启动客户端 → 自动检测到新版本 → 用户点【立即更新】
6. 客户端自动下载 → 替换 → 重启
```

整个过程**用户不需要重新安装**，零操作升级。

---

## 一、改版本号（每次必须）

编辑 `D:\bs\xhs_scraper\license_mgr.py` 第 24 行：

```python
CLIENT_VERSION = "v2.7"   # 改成你这次的版本号
```

也同步改 `main.py` 第 37 行的标题：

```python
root.title(f"绮绮采集器（仅供学习）v2.7  |  ...")
```

和 `activation_ui.py` 第 13 行：

```python
VERSION = "v2.7"
```

> 版本号约定：v主.次[.补丁]，递增比较，新版本必须严格大于老版本
> ✅ v2.6 → v2.7 (OK)
> ✅ v2.7 → v2.7.1 (OK)
> ❌ v2.7 → v2.7 (服务器拒绝重复)

---

## 二、打包

```bat
cd D:\bs\xhs_scraper
build_exe.bat
```

等 2-5 分钟，得到：
```
dist\QiQiCollector.exe
```

---

## 三、发布到服务器（任选一种方式）

### 方式 A: 在管理后台上传（推荐，可视化）

1. 打开 `http://60.204.204.181:5000/admin`
2. 进 **🆕 版本发布** Tab
3. 填表单：
   - **版本号**: `v2.7`
   - **最低兼容版本**: 留空（除非这个版本有重大变化必须升级）
   - **强制升级**: 不勾（除非有严重 bug 修复）
   - **更新内容**:
     ```
     - 新增 XX 功能
     - 修复 YY 问题
     - 优化 ZZ 性能
     ```
   - **EXE 文件**: 选 `dist\QiQiCollector.exe`
4. 点 **📤 发布**

发布成功后：
- 文件存在服务器 `/www/wwwroot/qiqi/releases/QiQiCollector_v2_7.exe`
- 所有运行中的客户端 5 分钟内会发现新版本
- 启动新客户端会立即提示更新

### 方式 B: 命令行上传（适合自动化）

```bash
# 在本地用 curl（或自己写脚本）
curl -X POST http://60.204.204.181:5000/admin/api/releases/upload \
  -b "session=<管理员cookie>" \
  -F "version=v2.7" \
  -F "changelog=- 修复xxx" \
  -F "file=@D:\bs\xhs_scraper\dist\QiQiCollector.exe"
```

(后台 cookie 比较麻烦，平时还是用方式 A)

---

## 四、用户侧体验

### 启动时自动检查
- 用户打开 exe → 后台静默查询服务器
- 有新版本 → 日志区出现 `🆕 发现新版本 v2.7...`
- **不会主动弹窗打扰**（除非是强制升级）

### 用户主动检查
- 菜单 **📢 公告 → 🆕 检查软件更新**
- 弹出对话框显示：
  - 当前版本 vs 最新版本
  - 完整更新日志
  - 文件大小
  - **【立即更新】** / **【稍后再说】** 按钮

### 点击更新后
1. 进度条显示下载进度
2. 下载完做 SHA256 校验（防被劫持改包）
3. "✓ 已下载，3 秒后自动重启"
4. 自动生成一个临时 .bat 脚本：
   ```
   wait → kill 旧 exe → 替换 → 启动新 exe
   ```
5. 用户全程只用看进度条

### 强制升级机制（高级）

发布版本时勾选 **强制升级**：
- 用户启动时**立即弹窗**，不能跳过【稍后再说】按钮被禁用
- 不更新就不能继续用

适合场景：
- 服务端 API 改了，老版本无法工作
- 严重安全漏洞
- 修复了支付/卡密相关的关键 bug

---

## 五、回滚

万一新版本有严重问题：

1. 后台 → 🆕 版本发布 → 找到出问题的版本 → 点【删除】
2. 已经升级到该版本的用户不受影响（他们要等下一个版本）
3. 还没升级的用户会看到上一个版本作为"最新"
4. 立刻发新版本修复（v2.7 出问题 → 发 v2.7.1）

---

## 六、注意事项

### 🛡 安全
- exe 上传时服务器自动算 SHA256 并存数据库
- 客户端下载后校验，不一致直接报错拒绝
- 防止 CDN 劫持、中间人攻击

### 💾 存储
- 旧版本 exe 一直留在 `/www/wwwroot/qiqi/releases/`
- 50MB 一个，留 10 个就 500MB
- 想清理：后台直接删除老版本即可

### 📊 灰度发布（暂未实现，TODO）
现在是"全量发布"。如果需要灰度（先发给 10% 用户测试），将来可以加个 `targets` 字段。

### 🔍 日志查看
- 服务端 `tail -f /www/wwwroot/qiqi/server.log` 看下载请求
- 客户端日志区会显示 `🆕 发现新版本 v2.7`

---

## 七、版本号管理建议

| 改了什么 | 版本号变化 |
|---|---|
| 修复小 bug | v2.6 → v2.6.1 |
| 新增小功能 | v2.6 → v2.7 |
| 重大重构 / API 不兼容 | v2.6 → v3.0 |
| 紧急修复（强制升级） | v2.6 → v2.6.1 + 勾强制 |

---

## 🎯 30 秒发版速查卡

```
1. 改 license_mgr.py 的 CLIENT_VERSION
2. build_exe.bat
3. 后台 → 🆕 版本发布 → 选文件 → 填版本号 → 上传
4. 完事
```
